<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:background="@color/white"
    android:paddingBottom="@dimen/activity_vertical_margin">


    <EditText
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/login_activity_itxt_username"
        android:background="@drawable/rounded_corner"
        android:layout_marginTop="110dp"
        android:textSize="35dp"
        android:elevation="5dp"
        android:gravity="center"
        android:textColor="@color/blue"
        android:hint="@string/username"
        android:layout_alignParentTop="true"
        android:layout_alignParentEnd="true"
        android:layout_alignParentStart="true" />

    <EditText
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:inputType="textPassword"
        android:ems="10"
        android:id="@+id/login_activity_ipwd_password"
        android:background="@drawable/rounded_corner"
        android:textSize="35dp"
        android:textColor="@color/blue"
        android:gravity="center"
        android:elevation="5dp"
        android:hint="@string/password"
        android:layout_below="@+id/login_activity_itxt_username"
        android:layout_alignParentStart="true"
        android:layout_marginTop="32dp" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/login"
        android:id="@+id/login_activity_btn_login"
        android:elevation="5dp"
        android:textColor="@color/white"
        android:layout_marginTop="61dp"
        android:layout_below="@+id/login_activity_ipwd_password"
        android:onClick="login"
        android:layout_centerHorizontal="true" />
</RelativeLayout>
